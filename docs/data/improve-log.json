{
	"runAt": "2026-09-21T13:48:00Z",
	"action": "opened-pr",
	"pr": null,
	"proposal": "Close the documented #461 follow-up: flip the norwegian-rights skill's Premier League line from 'TV 2 Play / TV 2 Sport Premium' to 'Viaplay', matching the already-shipped load-bearing code (scripts/lib/norwegian-rights.js ~line 174 + authority.json football:premier-league, both Viaplay). The skill is read by the research/verify agents to fill the where-to-watch field, so the stale line was actively mis-guiding them toward the wrong PL channel — the exact summary↔streaming mismatch #461 fixed in code.",
	"evidence": [
		"improve-log.json (previous run, 2026-09-14, PR #461) notes[0]: the SKILL.md PL line (line 39: 'Premier League → TV 2 Play') 'should also flip to Viaplay for agent-guidance consistency, but that write was harness-permission-gated in this run … the skill line is a documentation follow-up.' It was never done — the gate persisted.",
		"research-log.json (2026-09-21T05:05:00Z) still lists 'PL authority.json-kanal TV 2→Viaplay' in its standing self-repair/improve flags; on inspection authority.json (line 805) and norwegian-rights.js (line 174) are already Viaplay, so the ONLY remaining stale PL=TV 2 reference in the repo was the skill line (verified: grep -rn 'premier league' across .claude/skills + scripts/agents returned exactly one hit).",
		"norwegian-rights SKILL.md Maintenance section (lines 89-92): 'When a verified event contradicts this map … update the relevant line in the same commit and date-stamp the change. Never leave a known-wrong entry standing.' The PL line contradicted the shipped code.",
		"tests/norwegian-rights.test.js line 15 already asserts 'Premier League → Viaplay'; the skill markdown is not pinned by any test, so the fix is safe. npm test: 88 files / 1537 tests pass."
	],
	"expectedEffect": "research/verify agents now read Viaplay (not TV 2 Play) as the PL prior, consistent with the deterministic map and authority.json. No agent hand-sets a PL streaming[] to TV 2 Play that the build then has to clobber back to Viaplay; the where-to-watch guidance is internally consistent across code + skill.",
	"howWeKnow": "verify-log stops re-correcting PL channels; research-log's standing 'PL channel' flag clears; no new summary↔streaming mismatch on PL events.",
	"notes": [
		"Pure documentation-consistency fix in a protected-adjacent-but-permitted path (.claude/skills/** is auto-mergeable — only .claude/settings.json is a protected path). No code or schema touched; the load-bearing fix already shipped in #461."
	]
}

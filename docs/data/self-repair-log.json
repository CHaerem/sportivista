{
	"runAt": "2026-08-25T07:35:00Z",
	"action": "opened-pr",
	"pr": "https://github.com/CHaerem/sportivista/pull/442",
	"diagnosed": "NEW real breakage: `npm test` was RED — tests/starter-packs.test.js (WP-162 guard) 'every starter-pack id is a LIVE entity in docs/data/entities.json' failed on 'esports-world-cup-cs2'. Root cause: the iOS «e-sport (CS2)» starter pack (ios/Sportivista/Onboarding/StarterPacks.swift) grounded on entity id `esports-world-cup-cs2`, which is EDITION-DERIVED — it only materialises in entities.json while tracked.json books a dated CS2 EWC edition (`esports-world-cup-2026-cs2` → seasonless `esports-world-cup-cs2`). Today's static pipeline pruned the finished 2026 CS2 edition (commit df785004d, 'Update sports data 05:28Z'), the entity vanished, and the shipped pack followed NOTHING. Verified with build-entities.js: OFF-season the durable primary id is `esports-world-cup` (from catalog.json tier2 coverage compass, folded every build); IN-season a tracked dated edition folds first and wins the id, demoting `esports-world-cup` to a mere alias. So the edition-scoped id is never a stable primary — it flips by season; the always-present id is the catalog's `esports-world-cup`.",
	"fixed": [
		"ios/Sportivista/Onboarding/StarterPacks.swift — CS2 pack now grounds on the durable catalog id `esports-world-cup` (the marquee tournament, always in the index) instead of the edition-derived `esports-world-cup-cs2`, matching how the football pack uses `eliteserien`/`norge` and the winter pack uses `sport-*` (WP-132: generic packs ground on broadly-meaningful, real entities). Added a rationale comment.",
		"ios/SportivistaTests/OnboardingTests.swift — test_cs2Pack expectation updated to [\"esports-world-cup\"].",
		"ios/SportivistaTests/Fixtures/entities.json — replaced the stale edition entity with the live-index `esports-world-cup` entity (1-for-1, fixture entity count unchanged), removing the fixture↔live drift the WP-162 test exists to catch."
	],
	"autoMergeEligible": true,
	"notes": [
		"PROOF: `npm test` → 1382/1382 green; tests/starter-packs.test.js all 4 assertions pass. All changes are under ios/, so ios-tests.yml gates PR #442 on macOS (the Swift suite can't run in the pipeline env). Not merged — opened for CI/review per the self-repair contract.",
		"KNOWN RESIDUAL (follow-up, out of a mechanic's safe scope): the season flip means if research re-books a discipline-suffixed edition (esports-world-cup-20xx-cs2) next season, the primary id becomes esports-world-cup-cs2 again and this guard would re-fail in-season. A durable end-state (unify discipline editions into the generic catalog id as altIds; have the WP-162 guard check id ∪ altIds i.e. resolvability) is a build-entities/coverage-model change that would ripple the golden feed-vectors — research/improve territory, not an unattended self-repair patch. Flagged in the PR body.",
		"PRE-EXISTING PROTECTED-PATH BREAKAGES (unchanged, still human-must-author — see 08-24 log): (1) research-agent.yml deep-tier escalation dies on allowed_bots (github-actions[bot] rejected by claude-code-action); (2) visual-qa-agent.yml hits error_max_turns at --max-turns 40. Both live under .github/workflows/**, which the protect-automation CI hook blocks this agent from writing. Not re-attempted this run. These account for the recurring 'Research agent' / 'Visual QA agent' failures in `gh run list`.",
		"Non-bugs ruled out: usage-state.json green (no quota attribution). validate-events.js → 0 errors (only known WP-246/WP-242/timezone warnings owned by research/verify). No open self-repair/ PR existed before this run (only wp-242b-strip-lookalikes was open)."
	]
}

{
	"runAt": "2026-08-21T07:28:00Z",
	"action": "abandoned",
	"pr": null,
	"diagnosed": "STILL BROKEN (2nd consecutive run to find this): deep-tier research escalation fails on every bot-initiated run. When scout / coverage-critic escalate via `gh workflow run research-agent.yml -f tier=deep` (dispatched with the job's GITHUB_TOKEN → actor github-actions[bot]), anthropics/claude-code-action@v1 refuses to run: 'Action failed with error: Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \"*\" to allow all bots.' The workflow sets no allowed_bots (default empty = no bots). Both deep steps hit it: the Fable 5 step (continue-on-error → silently no-ops, so 'Did Fable 5 do the work?' sees no commit) and the Opus 4.8 fallback (no continue-on-error → fails the whole job). Confirmed against run history: schedule-triggered standard runs succeed (human actor), workflow_dispatch bot escalations fail — runs 32447688550 (2026-08-21 04:39), 32332683317 (2026-08-20 04:39), 32116501845, 31935302839. Scheduled research is unaffected; the scout/coverage-critic → deep-research fast lane is fully dead.",
	"fixed": [],
	"autoMergeEligible": false,
	"notes": [
		"NOT FIXED — the only correct fix lives entirely in a protected automation path (.github/workflows/research-agent.yml). The CI-only protect-automation hook (scripts/hooks/protect-automation.js) blocks this unattended agent from writing it (exit 2: 'A human must author changes to these files'), so no branch/PR could even be created. No non-protected workaround exists: allowed_bots must be an input to the claude-code-action steps; scripts/escalate-research.js cannot influence the action's actor check (using a PAT to make the dispatch a human actor would itself require a protected workflow/secret change).",
		"READY-TO-APPLY FIX (needs a human, ~3 lines): add `allowed_bots: \"github-actions[bot]\"` under `with:` on all three `uses: anthropics/claude-code-action@v1` steps in .github/workflows/research-agent.yml — the standard Opus 4.8 step (~line 80), the deep Fable 5 step (~line 103), and the deep Opus 4.8 fallback (~line 131). tier=standard is escalatable too, so all three need it. Scope to the exact actor github-actions[bot] rather than '*': the action's own docs warn that '*' on a PUBLIC repo lets external Apps invoke it with prompts they control — but scoping to our own bot is safe here because the prompt is a hardcoded file reference and the only workflow_dispatch input is the `tier` choice, so there is no external-prompt-injection surface.",
		"This is the SECOND consecutive self-repair run to diagnose this exact break and be blocked by the hook (see the prior 2026-08-20 log entry). The docs/data/self-repair-log.json channel has not reached a human operator; a louder surfacing (a GitHub issue, the same pattern scripts/escalate-research.js uses for dispatch failures) may be warranted if it persists another run.",
		"Non-bugs ruled out this run: full `npm test` 1376/1376 green; `node scripts/validate-events.js` 0 errors (warnings only: timezone-bleed, landing-page channel URLs WP-246, weak time/channel basis WP-242 — all data-quality signals owned by research/verify, not mechanic breakage); quota green (usage-state.json week 54%, session allowed). The 2026-08-19 web-tests CI failure (sources/authority id-sort ordering in authority-schema.test.js:92 + sources-schema.test.js:111) is already resolved — both tests pass now, self-healed by a later config rewrite. The Visual QA failures (32116501845, 32349103634) are a SEPARATE, unrelated issue: 'Claude reported a successful result after 45 turns, exceeding the configured maximum of 40' (visual-qa max-turns=40 too low); optional agent, lower impact, not fixed here."
	]
}

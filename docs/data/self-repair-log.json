{
	"runAt": "2026-09-21T13:12:00Z",
	"action": "abandoned",
	"pr": null,
	"diagnosed": "The Research agent's deep-tier ESCALATION path is 100% broken. Every workflow_dispatch-triggered run fails; every schedule-triggered run succeeds. Scout and coverage-critic escalate via `gh workflow run research-agent.yml -f tier=deep`, so the dispatch actor is github-actions[bot]. claude-code-action@v1 refuses bot-initiated runs unless the bot is allow-listed in its `allowed_bots` input (default \"\" = no bots), failing with: 'Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \\'*\\' to allow all bots.' Confirmed across runs 35592238892 + 35584767701 (both TODAY), 35464837752, 35435479244, 35406257399, 35395748434, 35332693173, 35326546418, 35239416470 — all workflow_dispatch, all this exact error on both the Fable-5 and Opus-4.8-fallback steps. This is NOT quota/transient (the 2026-09-18 self-repair run misread it as such): usage-state.json is green (session 3%, week 4%), and the split is deterministic by trigger, not intermittent.",
	"fixed": [],
	"autoMergeEligible": false,
	"notes": [
		"FIX (human must author — protected path): add `allowed_bots: \"github-actions[bot]\"` as a top-level `with:` input alongside claude_code_oauth_token on each anthropics/claude-code-action@v1 step in .github/workflows/research-agent.yml (the standard, Fable-5 deep, and Opus-4.8 deep-fallback steps). Scope it to the one bot, not \"*\", to avoid the public-repo external-App risk the action warns about. This restores the scout/coverage-critic -> deep-research escalation loop.",
		"Could not open a PR: .github/workflows/** is a harness-protected automation path. The protect-automation.js PreToolUse hook hard-blocks any CI agent from writing it (exit 2) — by design a human must author workflow changes. Every fix for this root cause lives in a workflow file (the workflow input, or how scout/coverage-critic dispatch), so there is no non-protected-path alternative. Did not attempt to circumvent the hook.",
		"Proof of diagnosis: `gh run list --workflow=research-agent.yml` shows workflow_dispatch=failure and schedule=success with no exceptions across the last 25 runs; `gh run view <id> --log-failed` on 35406257399 shows the allowed_bots error on both deep steps.",
		"Other health checks clean: full npm test 1537/1537 pass; node scripts/validate-events.js 0 errors (warnings only); publish-freshness.json status=fresh (lag 0h); usage-state.json green. The 2026-09-17 CI failure (authority-schema.test.js) is already resolved on main.",
		"Visual QA agent failures are a separate, intermittent max-turns (40) cap, not deterministic breakage — its last 3 scheduled runs passed. Left for the improve agent (threshold tuning), consistent with self-repair scope."
	]
}

{
  "runAt": "2026-09-06T11:12:00.000Z",
  "action": "abandoned",
  "pr": null,
  "diagnosed": "Repo-side health is green: full `npm test` 1537/1537 (88 files) pass; `node scripts/validate-events.js` validates 154 events with 0 errors (only warnings); usage-state.json green (session 24%, week 63%); publish-freshness.json 'fresh' (lag 0h, live==repo 07:28:53Z). Two REAL, still-live breakages remain — both identical to the 2026-09-01/02/03/04/05 logs and both confined to protected workflow files. (1) Escalation-triggered Research runs fail 100%: every workflow_dispatch research run dies (latest 34021884104, 2026-09-06 08:27Z; also 33734273496 09-03, 33524225428 09-01) while every `schedule` run passes, because .github/workflows/research-agent.yml's three claude-code-action steps have no `allowed_bots` and the escalation (scout/coverage-critic → scripts/escalate-research.js → `gh workflow run` with the job GITHUB_TOKEN) makes the actor github-actions[bot], which claude-code-action@v1 rejects: 'Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \"*\" to allow all bots.' The whole scout/coverage-critic → deep-research escalation path is dead. (2) Visual QA runs fail every run since 2026-08-31 (33963574557 09-05, 33872701702 09-04, 33755381403 09-03, 33629974246 09-02, 33524... and 33491173346) with turn exhaustion — latest 'Claude reported a successful result after 52 turns, exceeding the configured maximum of 40'; visual-qa-agent.yml pins `--max-turns 40`. Config unchanged, so this is agent/model-side turn drift, not a code regression.",
  "fixed": [],
  "autoMergeEligible": false,
  "notes": [
    "NOT AUTHORED (protected path). Both real fixes touch .github/workflows/**, which is human-only and HARD-BLOCKED at write time by scripts/hooks/protect-automation.js (fired on the Edit attempt this run; CI-gated, and its docstring is explicit that it applies to EVERY CI agent incl. the self-fixing loops — an unattended agent may not even author these on a branch). So no self-repair PR is possible; these must be authored by the owner. This matches the 2026-09-05 abandoned run — the breakage is persistent because the mechanic cannot author the only correct fix.",
    "FIX 1 (research escalation): in .github/workflows/research-agent.yml add `allowed_bots: \"github-actions[bot]\"` under `claude_code_oauth_token:` in ALL THREE claude-code-action steps (standard, deep-Fable, deep-Opus-fallback). The action lowercases + strips `[bot]`, so `github-actions[bot]` matches the github-actions actor (`github-actions` without the suffix is equivalent). Prefer the specific bot over `\"*\"` — this is a PUBLIC repo. Scheduled/manual runs have a User actor for whom allowed_bots is ignored, so the change is safe for the passing paths.",
    "FIX 1 is INVISIBLE to the existing alarm: scripts/escalate-research.js only raises the `escalation-failed` issue when the DISPATCH fails, but the dispatch succeeds — it is the dispatched RUN that dies. Non-protected improve-eligible follow-up: have escalate-research.js additionally poll the dispatched run's conclusion so a rejected run becomes loud instead of silent.",
    "FIX 2 (Visual QA max-turns): raise `--max-turns` in .github/workflows/visual-qa-agent.yml (e.g. 40→60) and/or tighten scripts/agents/visual-qa.md so the run finishes inside budget. Also human-only (protected workflow).",
    "No non-protected code breakage found this run — tests and validation are clean. The historical CI failures on main (09-02/09-03, e.g. 33716066944) were research-agent tracked.json schema violations pushed direct to main; they self-healed on later research runs (tracked-schema.test.js passes now)."
  ]
}

{
  "runAt": "2026-09-08T11:37:00.000Z",
  "action": "abandoned",
  "pr": null,
  "diagnosed": "Repo-side health is green: full `npm test` 1537/1537 (88 files) pass; `node scripts/validate-events.js` validates 170 events with 0 errors (warnings only); usage-state.json green (session 37%, week 30% — so the failures are NOT quota-related); publish-freshness.json 'fresh' (lag 0h, live==repo 2026-09-08T07:38:56Z). Two REAL, still-live breakages remain — both identical to the 2026-09-01…09-07 logs and both confined to protected workflow files. (1) Escalation-triggered Research runs fail 100%: every workflow_dispatch research run dies (latest 34205960728 2026-09-08 08:42Z; also 34103552555 09-07, 34021884104 09-06, 33734273496 09-03, 33524225428 09-01, 33491173346 09-01) while every `schedule` run passes, because .github/workflows/research-agent.yml's three claude-code-action steps have no `allowed_bots` and the escalation (scout/coverage-critic → scripts/escalate-research.js → `gh workflow run research-agent.yml -f tier=deep` with the job GITHUB_TOKEN) makes the actor github-actions[bot], which claude-code-action@v1 rejects: 'Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \"*\" to allow all bots.' The whole scout/coverage-critic → deep-research escalation path is dead. (2) Visual QA runs fail every `schedule` run since 2026-08-31 (latest 34129807978 09-07, also 34031469059 09-06, 33963574557 09-05, 33872701702 09-04, 33755381403 09-03, 33629974246 09-02) with turn exhaustion — 'Claude reported a successful result after 46 turns, exceeding the configured maximum of 40'; visual-qa-agent.yml pins `--max-turns 40`. The run completes the QA work in ~3.5 min then fails purely on the turn cap; config unchanged, so this is agent/model-side turn drift, not a code regression.",
  "fixed": [],
  "autoMergeEligible": false,
  "notes": [
    "NOT AUTHORED (protected path). Both real fixes touch .github/workflows/**, which is human-only and HARD-BLOCKED at write time by scripts/hooks/protect-automation.js (fired on the Edit attempt this run; CI-gated via GITHUB_ACTIONS/CI, and its docstring is explicit that it applies to EVERY CI agent incl. the self-fixing loops — an unattended agent may not even author these on a branch). So no self-repair PR is possible; these must be authored by the owner. This confirms the 2026-09-05…09-07 abandoned runs — the breakage is persistent because the mechanic cannot author the only correct fix.",
    "FIX 1 (research escalation): in .github/workflows/research-agent.yml add an `allowed_bots` input under `claude_code_oauth_token:` in the two DEEP-tier claude-code-action steps (Fable-5 preferred + Opus-4.8 fallback) — escalations only run the deep tier; adding it to the standard step too is harmless and consistent. Value: `\"github-actions\"` (per claude-code-action's actor normalization the `[bot]` suffix is stripped) — prefer the specific bot over `\"*\"` on this PUBLIC repo (the prompt is hardcoded in the workflow, so there is no external-prompt-injection surface either way). Scheduled/manual runs have a User actor for whom allowed_bots is ignored, so the change is safe for the currently-passing schedule path.",
    "FIX 1 is INVISIBLE to the existing alarm: scripts/escalate-research.js only raises the `escalation-failed` issue when the DISPATCH fails, but the dispatch succeeds — it is the dispatched RUN that then dies on the actor check. A non-protected, improve-eligible follow-up (NOT authored here — observability, not breakage): have escalate-research.js additionally poll the dispatched run's conclusion so a rejected run becomes loud instead of silently failing.",
    "FIX 2 (Visual QA turn cap): raise `--max-turns` in .github/workflows/visual-qa-agent.yml (e.g. 40→60; the run finishes the actual QA in ~3.5 min well inside the 15-min job timeout, so headroom is cheap) and/or tighten scripts/agents/visual-qa.md so the run finishes inside budget. Also human-only (protected workflow).",
    "No non-protected code breakage found this run — tests and validation are clean. Older CI failures on main (09-02/09-03, e.g. 33716066944) were a transient tracked.json schema violation pushed direct to main by the research agent; it self-healed on a later research run (tracked-schema.test.js passes now)."
  ]
}

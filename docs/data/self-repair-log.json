{
	"runAt": "2026-09-25T12:10:00Z",
	"action": "abandoned",
	"pr": null,
	"diagnosed": "REPEAT FINDING (still unfixed since 2026-09-21/2026-09-22): the Research agent's ESCALATION path is 100% broken. Of the last 20 research runs every `schedule` run succeeds and every `workflow_dispatch` run fails (confirmed today: 2026-09-24T15:31 dispatch=failure; the Sep 25 scheduled runs all pass). Scout and coverage-critic escalate via `gh workflow run research-agent.yml -f tier=deep` (scripts/escalate-research.js), so the resulting workflow_dispatch run is initiated by the github-actions bot. claude-code-action@v1 now refuses bot-initiated runs unless the bot is allow-listed in its `allowed_bots` input, failing with: 'Action failed with error: Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \\'*\\' to allow all bots.' Confirmed on run 36020768893 — both the Fable-5 deep step and the Opus-4.8 deep-fallback step show the exact error (the failing step is 'Research — Opus 4.8 (deep fallback)'). NOT quota/transient: usage-state.json is green (session 4%, week 33%), the split is deterministic by trigger (not intermittent), and the dispatch itself reports success so escalations fail silently — the scout/coverage-critic -> deep-research recall loop is dead.",
	"fixed": [],
	"autoMergeEligible": false,
	"notes": [
		"FIX (human must author — protected path): add an `allowed_bots` input alongside `claude_code_oauth_token` on each anthropics/claude-code-action@v1 step in .github/workflows/research-agent.yml (the standard, the Fable-5 deep, and the Opus-4.8 deep-fallback steps). The two deep steps are the ones reached by escalation; adding it to all three keeps the invocations uniform and guards a future bot-dispatched standard run. Use `allowed_bots: \"github-actions\"` (the actor name the error reports) — scope it to the one bot rather than \"*\" to avoid allowing arbitrary external bots on this public repo. If the action's matcher expects the full login, `\"github-actions[bot]\"` or `\"*\"` are the fallbacks. This restores the scout/coverage-critic -> deep-research escalation loop.",
		"Could not open a PR: .github/workflows/** is a harness-protected automation path. The protect-automation.js PreToolUse hook hard-blocks any CI agent from writing it (exit 2, re-confirmed this run: the Edit was BLOCKED under GITHUB_ACTIONS/CI) — by design a human must author workflow changes. Every fix for this root cause lives in a workflow file (the action input, or how the escalation dispatches), so there is no non-protected-path alternative. Did not attempt to circumvent the hook. NOTE for the owner: the self-repair prompt's 'make the protected-path fix anyway, it'll wait for review' guidance is now superseded by this hook — the workflow edit must be applied by a human.",
		"Proof of diagnosis: `gh run list --workflow='Research agent' --limit 20` shows workflow_dispatch=failure and schedule=success with no exceptions; `gh run view 36020768893 --log | grep 'non-human actor'` shows the allowed_bots error on the deep steps; `grep allowed_bots .github/workflows/*.yml` = no matches (still unset).",
		"Other health checks clean: full `npm test` 1541/1541 pass (88 files); `node scripts/validate-events.js` 0 errors (WP-242/246 warnings only); publish-freshness.json status=fresh (lag 0h, live==repo at 2026-09-25T08:25:08Z); usage-state.json green; no open self-repair/ PRs. The 2026-09-17 CI failure (authority-schema.test.js chess:grand-chess-tour sort/aggregator-channel) is already resolved on main.",
		"Visual QA agent failures (Sep 15-17) are separate and intermittent, not deterministic breakage; its scheduled runs otherwise pass. Left for the improve agent (threshold/turn tuning), consistent with self-repair scope."
	]
}

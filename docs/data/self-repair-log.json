{
	"runAt": "2026-09-22T11:59:58Z",
	"action": "abandoned",
	"pr": null,
	"diagnosed": "REPEAT FINDING (still unfixed since the 2026-09-21 run): the Research agent's ESCALATION path is 100% broken. Across the last 15 research runs every `schedule` run succeeds and every `workflow_dispatch` run fails. Scout and coverage-critic escalate via the static pipeline running `gh workflow run research-agent.yml -f tier=deep` (scripts/escalate-research.js), so the resulting workflow_dispatch run is initiated by the github-actions bot. claude-code-action@v1 now refuses bot-initiated runs unless the bot is allow-listed in its `allowed_bots` input (default \"\" = no bots), failing with: 'Action failed with error: Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \\'*\\' to allow all bots.' Confirmed TODAY on runs 35592238892 + 35584767701 (both the Fable-5 deep and Opus-4.8 deep-fallback steps show the exact error). This is NOT quota/transient: usage-state.json is green (session 1%, week 16%), the split is deterministic by trigger (not intermittent), and the dispatch itself reports success so escalations fail silently — the scout/coverage-critic -> deep-research recall loop is dead.",
	"fixed": [],
	"autoMergeEligible": false,
	"notes": [
		"FIX (human must author — protected path): add an `allowed_bots` input alongside `claude_code_oauth_token` on each anthropics/claude-code-action@v1 step in .github/workflows/research-agent.yml (the standard, the Fable-5 deep, and the Opus-4.8 deep-fallback steps). The two deep steps are the ones reached by escalation; adding it to all three keeps the invocations uniform and guards against a future bot-dispatched standard run. Use `allowed_bots: \"github-actions\"` (the actor name the error reports) — scope it to the one bot rather than \"*\" to avoid allowing arbitrary external bots on this public repo. If the action's matcher expects the full login, `\"github-actions[bot]\"` or `\"*\"` are the fallbacks. This restores the scout/coverage-critic -> deep-research escalation loop.",
		"Could not open a PR: .github/workflows/** is a harness-protected automation path. The protect-automation.js PreToolUse hook hard-blocks any CI agent from writing it (exit 2, confirmed this run: GITHUB_ACTIONS=true/CI=true) — by design a human must author workflow changes. Every fix for this root cause lives in a workflow file (the workflow input, or how the escalation dispatches), so there is no non-protected-path alternative. Did not attempt to circumvent the hook.",
		"Proof of diagnosis: `gh run list --workflow=research-agent.yml --limit 15` shows workflow_dispatch=failure and schedule=success with no exceptions; `gh run view 35592238892 --log` shows the allowed_bots error on the deep steps.",
		"Other health checks clean: full `npm test` 1541/1541 pass; `node scripts/validate-events.js` 0 errors (WP-242/246 warnings only); publish-freshness.json status=fresh (lag 0h); usage-state.json green; no open self-repair/ PRs. The 2026-09-17 CI failure (authority-schema.test.js) is already resolved on main.",
		"Visual QA agent failures are a separate, intermittent max-turns (40) cap, not deterministic breakage — its scheduled runs otherwise pass. Left for the improve agent (threshold tuning), consistent with self-repair scope."
	]
}

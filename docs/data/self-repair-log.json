{
	"runAt": "2026-08-17T07:33:53Z",
	"action": "abandoned",
	"pr": null,
	"diagnosed": "ONE real, current, recurring breakage — but its only correct fix lands in a protected `.github/workflows/**` path that the protect-automation.js PreToolUse hook BLOCKS this agent from writing (re-confirmed live this run: the Edit adding `allowed_bots` to research-agent.yml was refused — 'BLOCKED by hook: … A human must author changes to these files'; the hook fired because this is a CI context). ESCALATION BOT-ACTOR: every `workflow_dispatch` research run now FAILS while every `schedule` run succeeds — 3 straight dispatch failures in the last 3 days (run 31935302839 08-16, 31902818550 08-15, 31833979342 08-14), each dying at claude-code-action@v1 with '##[error]Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \\'*\\' to allow all bots.' Root cause is definitive and downstream of the WP-91 dispatch fix: scout/coverage-critic escalations call `gh workflow run research-agent.yml` with the job's real GITHUB_TOKEN (actor = github-actions bot); the three claude-code-action@v1 steps in research-agent.yml carry no `allowed_bots` (grep -c allowed_bots = 0), so the action aborts before doing any research. escalate-research.js itself is healthy — the `gh workflow run` dispatch returns 0; the rejection is inside the dispatched run, so there is NO non-protected fix. This is the same issue the 08-13 run flagged, now with fresh proof it fires on EVERY escalation, not just latently.",
	"fixed": [],
	"autoMergeEligible": false,
	"notes": [
		"NO PR OPENED — the only correct fix is authorable solely by a human. It lands in `.github/workflows/research-agent.yml` (a protected automation path); the protect-automation.js PreToolUse hook BLOCKED the Edit this run, which is the intended invariant (CI-only hook, CI context). I did NOT circumvent it (no `gh api` contents PUT, no `git apply`/`patch`/redirect bypass) — that is exactly the threat the hook defends against. A known-broken fast-lane beats a fix pushed around a safety hook.",
		"READY-TO-APPLY PATCH (for a human): in .github/workflows/research-agent.yml, add `allowed_bots: \"github-actions\"` to the `with:` block of ALL THREE claude-code-action@v1 steps — Opus 4.8 standard (~line 79), Fable 5 deep (~line 102), and Opus 4.8 deep fallback (~line 130). Scope to `github-actions` (our own bot), NOT `*`, so no third-party bot can trigger a Claude run. Protected-path change → waits for review, never auto-merges. No test pins this (grep allowed_bots tests/ is empty); `npm test` stays green.",
		"WHY IT MATTERS: with escalation dead, scout (RSS/coverage-gap triage) and coverage-critic (recall audit) can DECIDE to pull the deep-tier research fast-lane but the pull is dropped on the floor — coverage gaps they spot only get picked up on the next 4-hourly scheduled research run, not on demand. The self-fixing coverage loop's fast path is down.",
		"OBSERVATION, NOT SHIPPED (Visual QA max-turns 40): the 08-13 run flagged an intermittent `error_max_turns` at `--max-turns 40` in visual-qa-agent.yml (still 40 today). Re-checked this run: Visual QA has succeeded 6 straight days (08-11..08-16), last failure 08-08 — no recurrence in 9 days, not reproducible locally, and also a protected-path change. Not a confirmed current break; left as an observation for the human reviewing the escalation patch to consider bumping to 60 while in that file.",
		"NON-BUGS RULED OUT: the older entries in `gh run list --status failure` are stale/transient — Research/Scout/Editorial/Usage-monitor 08-06 were conclusion=cancelled (concurrency noise); iOS Release 08-06 detect=cancelled; Visual QA 08-08 already cured by 6 later successes. The repeated 'Static data pipeline' cancellations are by-design concurrency serialisation (data commits ARE landing — e.g. 'Update sports data 06:48Z'), not breakage.",
		"HEALTH CHECK GREEN otherwise: `npm test` 1373/1373 (81 files); `node scripts/validate-events.js` 0 errors (77 events; only the expected WP-242/WP-246 content warnings the research/verify agents own + a chess timezone-bleed note); build-alert.json ok (77 events, 06:48Z); usage-state.json green (session 5%, week 2% — not a quota skip).",
		"This log was written to the main working tree during a user-invoked run; it was NOT committed or pushed (hard constraint: never push to main; branch+PR only). The scheduled self-repair workflow's 'Commit run log' step is what normally persists this file."
	]
}

{
  "runAt": "2026-09-04T11:45:00.000Z",
  "action": "abandoned",
  "pr": null,
  "diagnosed": "Repo-side health is green: full npm test 1525/1525 (87 files) pass, node scripts/validate-events.js reports 0 errors (only pre-existing WP-242/WP-246/timezone-bleed warnings), usage-state.json green (session 0%, week 16%), publish-freshness.json 'fresh' (lag 0h), build-alert.json ok. The 2026-09-02/03 CI reds (tracked.json `notes` shape) were already fixed by PR #458 (merged 2026-09-03T11:36Z); CI on main has been green since (runs at 13:11Z, 22:49Z, 04:47Z). The one real, still-live breakage: escalation-triggered Research runs FAIL 100%. Every workflow_dispatch research run dies (33734273496 today 08:36Z, plus 33524225428, 33491173346, 33383194226, 33304960129 …) while every schedule run passes — because .github/workflows/research-agent.yml's three claude-code-action steps have no `allowed_bots`, and the escalation (scout/coverage-critic → scripts/escalate-research.js → `gh workflow run` with GITHUB_TOKEN) makes the triggering actor github-actions[bot], which the action rejects: 'Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \"*\".' The WP-91 fix made the DISPATCH succeed, so escalate-research.js sees success and never raises its escalation-failed alarm — the failure is invisible to that mechanism, so the scout/coverage-critic deep-research fast lane has been silently dead.",
  "fixed": [],
  "autoMergeEligible": false,
  "notes": [
    "NOT AUTHORED (protected path — .github/workflows/** is human-only, enforced by scripts/hooks/protect-automation.js which hard-blocked the edit; unchanged from the 2026-09-01/02/03 logs). One-line fix: in .github/workflows/research-agent.yml add `allowed_bots: \"github-actions\"` under `claude_code_oauth_token:` in ALL THREE claude-code-action steps (standard, deep-Fable, deep-Opus-fallback). `github-actions` is precise — that is the only bot that dispatches this workflow (via escalate-research.js). `\"*\"` also works but is broader.",
    "This escalation failure is INVISIBLE to the existing alarm: scripts/escalate-research.js only raises the `escalation-failed` GitHub issue when the DISPATCH fails, but the dispatch succeeds — it's the dispatched RUN that dies. Consider making escalate-research (a non-protected script) additionally verify the dispatched run's conclusion, or the owner watching `gh run list --workflow=research-agent.yml --event=workflow_dispatch` for failures.",
    "Also outstanding (protected workflow file, human-only, unchanged): Visual QA runs keep failing with 'Reached maximum number of turns (40)' (33755381403, 33629974246, 33524225428 …). Fix = raise --max-turns in .github/workflows/visual-qa-agent.yml, or tighten scripts/agents/visual-qa.md so the run finishes inside the budget.",
    "No PR opened: the only live breakage is confined to protected automation files an unattended agent may not author. Repo tests/validation/data all green; nothing else to repair."
  ]
}

{
  "runAt": "2026-09-13T12:14:00.000Z",
  "action": "abandoned",
  "pr": null,
  "diagnosed": "Repo-side health is green: full `npm test` 1537/1537 (88 files) pass; `node scripts/validate-events.js` validates 164 events with 0 errors (warnings only: PL Viaplay↔TV2-Play summary/streaming mismatch on 9 events, Presidents Cup timezone bleed, landing-page-only channel URLs on 42 near-term events, streaming missing on 1, weak time basis on 5 + weak channel basis on 3 high-confidence AI-research events — all research/verify CONTENT concerns, not machine breakage); usage-state.json green (session 0%, week 55% — failures are NOT quota-related); publish-freshness.json 'fresh' (lag 0h, live==repo 2026-09-13T07:52:43Z); no open self-repair/ PR. Two REAL, still-live breakages remain — both identical to the 2026-09-01…09-12 logs and both confined to protected workflow files. (1) Escalation-triggered Research runs fail 100%: every `workflow_dispatch` research run dies (latest 34712145720 2026-09-12 18:45Z; also 34648612959 09-11 21:17Z, 34330533276 09-09 08:41Z, 34287098447 09-08, 34205960728 09-08, 34021884104 09-06) while `schedule` runs pass, because .github/workflows/research-agent.yml's three claude-code-action steps have no `allowed_bots` and the escalation (scout/coverage-critic → scripts/escalate-research.js → `gh workflow run research-agent.yml -f tier=deep` with the job GITHUB_TOKEN) makes the dispatched-run actor github-actions[bot] (confirmed: event==workflow_dispatch, error 'Action failed with error: Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \"*\" to allow all bots.'; the deep tier's Fable-5 step AND its Opus-4.8 fallback both die with the same error). The whole scout/coverage-critic → deep-research escalation fast lane is dead. (2) Visual QA runs fail every `schedule` run (latest 34692209230 09-12 11:57Z, also 34599211580 09-11, 34477700504 09-10, 34352120636 09-09) with turn exhaustion — 'Execution failed: Reached maximum number of turns (40)'; visual-qa-agent.yml pins `--max-turns 40`. The agent completes its work but past the cap, so this is turn-budget drift, not a code regression.",
  "fixed": [],
  "autoMergeEligible": false,
  "notes": [
    "NOT AUTHORED (protected path). Both real fixes touch .github/workflows/**, which is human-only and HARD-BLOCKED at write time by scripts/hooks/protect-automation.js — it fired again on the Edit attempt this run (I created branch self-repair/20260913-1213, tried to add allowed_bots to research-agent.yml, got 'BLOCKED by hook: that path is a protected automation path', made no change, discarded the branch, returned to main). The hook is CI-gated (GITHUB_ACTIONS/CI) and applies to EVERY unattended agent incl. the self-fixing loops, so the mechanic cannot even author these on a branch. No self-repair PR is possible; these must be authored by the owner. This confirms the 2026-09-05…09-12 abandoned runs — the breakage persists precisely because the mechanic cannot author the only correct fix. self-repair.md's 'if the only correct fix touches a protected path, still make it' guidance is defeated by the stricter protect-automation hook; the two contracts disagree and only a human can resolve it (either relax the hook to allow authoring-but-not-merging protected-path fixes, or the owner applies the one-liners below).",
    "FIX 1 (research escalation) — owner one-liner: in .github/workflows/research-agent.yml add `allowed_bots: \"github-actions\"` alongside `claude_code_oauth_token:` in the claude-code-action steps. Escalations only run the deep tier, so the two DEEP-tier steps (Fable-5 preferred + Opus-4.8 fallback) are load-bearing; adding it to the standard step too is harmless and consistent (escalate-research.js also accepts tier=standard). Prefer the specific bot over `\"*\"`: this is a PUBLIC repo and the action's own docs warn `*` lets external Apps invoke the action with prompts they control; the explicit bot is more defensible (workflow_dispatch already requires write access, so no external bot can dispatch). Confirmed against the current action.yml: `allowed_bots` is a valid input (default empty = allow no bots); its compare lowercases and strips a trailing `[bot]` on both sides, so `\"github-actions\"` matches the github-actions[bot] actor. Scheduled/manual runs have a User actor for whom allowed_bots is ignored, so the change is safe for the currently-passing schedule path. Guaranteed fallback per the action's own error text is `allowed_bots: \"*\"`.",
    "FIX 1 is INVISIBLE to the existing alarm: scripts/escalate-research.js only raises the `escalation-failed` issue when the DISPATCH fails, but here the dispatch SUCCEEDS — it is the dispatched RUN that then dies on the actor check. So no alarm has fired for 10+ days. A non-protected, improve-eligible follow-up (NOT breakage, so not authored here): have escalate-research.js additionally poll the dispatched run's conclusion so a rejected run becomes loud instead of silently failing.",
    "FIX 2 (Visual QA turn cap): raise `--max-turns` in .github/workflows/visual-qa-agent.yml (e.g. 40→80; the 15-min job timeout gives ample headroom and prior runs show the agent finishing its work just past the cap) and/or tighten scripts/agents/visual-qa.md so the run finishes inside budget. Also human-only (protected workflow).",
    "No non-protected code breakage found this run — tests and validation are clean. The static pipeline, publish-freshness, and usage governor are all healthy."
  ]
}

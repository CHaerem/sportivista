{
	"runAt": "2026-09-26T11:45:00Z",
	"action": "abandoned",
	"pr": null,
	"diagnosed": "REPEAT FINDING (open, human-only fix, unchanged since 2026-07-30 — tracking issue #430): the Research agent's ESCALATION path is 100% broken. Of the last 20 research runs every `schedule` run succeeds and every `workflow_dispatch` run fails. Re-confirmed today on the most recent escalation, run 36020768893 (workflow_dispatch, 2026-09-24T15:31Z): both the Fable-5 deep step and the Opus-4.8 'deep fallback' step fail with 'Action failed with error: Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use \\'*\\' to allow all bots.' Scout and coverage-critic escalate via `gh workflow run research-agent.yml -f tier=deep` (scripts/escalate-research.js) using the job's GITHUB_TOKEN, so the dispatched run's actor is github-actions[bot], which claude-code-action@v1 blocks unless allow-listed via its `allowed_bots` input. NOT quota/transient: usage-state.json is green (session 0%, week 52%), the failure is deterministic by trigger (dispatch fails, schedule passes, no exceptions), and the dispatch call itself reports success so escalations fail SILENTLY — the scout/coverage-critic -> deep-research recall fast-lane is dead.",
	"fixed": [],
	"autoMergeEligible": false,
	"notes": [
		"FIX (human must author — protected path): add `allowed_bots: \"github-actions\"` alongside `claude_code_oauth_token` on each anthropics/claude-code-action@v1 step in .github/workflows/research-agent.yml (the standard, the Fable-5 deep, and the Opus-4.8 deep-fallback steps). Scope to the one bot login `github-actions` (the actor the error reports), NOT `*` — this is a public repo and `*` would let arbitrary external Apps invoke the action. The two deep steps are the ones reached by escalation; adding it to all three keeps the invocations uniform. This restores the scout/coverage-critic -> deep-research escalation loop. Full ready-to-apply diff is in issue #430.",
		"Could NOT open a PR: .github/workflows/** is a harness-protected automation path. The protect-automation.js PreToolUse hook hard-blocks any CI agent from writing it (exit 2, re-confirmed this run: my Edit to research-agent.yml was BLOCKED under GITHUB_ACTIONS). By design a human must author workflow changes. Every fix for this root cause lives in a workflow file, so there is no non-protected-path alternative. Did not attempt to circumvent the hook. The self-repair prompt's 'make the protected-path fix anyway, it'll wait for review' guidance is superseded by this hook — the workflow edit must be applied by a human.",
		"Owner already notified: tracking issue #430 (labelled `self-repair`, open since 2026-07-30) documents the identical root cause + fix. Added a fresh dated re-confirmation comment today (the prior confirmation was 2026-08-02, ~8 weeks ago) so the owner has current evidence it persisted through September and did not self-resolve.",
		"Proof of diagnosis: `gh run list --workflow='Research agent' --limit 20` shows workflow_dispatch=failure and schedule=success with no exceptions; `gh run view 36020768893 --log | grep 'non-human actor'` shows the allowed_bots error on both deep steps; `grep allowed_bots .github/workflows/*.yml` = no matches (still unset).",
		"Other health checks clean this run: full `npm test` 1541/1541 pass (88 files); `node scripts/validate-events.js` 0 errors (WP-242/246 near-term warnings only, non-blocking); publish-freshness.json status=fresh (lag 0h, live==repo at 2026-09-26T08:12:17Z); usage-state.json green; no open self-repair/ PRs.",
		"Other recent failures are out of self-repair scope: the older 2026-09-17 CI failures are already resolved on main (tests green today); the Sep 15-17 Visual QA agent failures are intermittent, not deterministic breakage (its scheduled runs otherwise pass) — left for the improve agent (threshold/turn tuning)."
	]
}

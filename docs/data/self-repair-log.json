{
	"runAt": "2026-08-22T07:12:00Z",
	"action": "opened-pr",
	"pr": "https://github.com/CHaerem/sportivista/pull/440",
	"diagnosed": "web-tests CI is red on main (run 32553390213, 2026-08-22 05:04): tests/authority-schema.test.js > 'konkurransene er sortert på id (ren diff ved endringer)' fails because scripts/config/authority.json has cycling:renewi-tour appended at the END of the competitions array (after tennis:tour) instead of sorted by id. Deterministic, reproduces locally (npm test → 1 failed / 1375 passed). This is the same id-sort contract that self-healed on 2026-08-19 and has now regressed again — an AI config rewrite added the new renewi-tour entry at the tail rather than in sorted position (it belongs between cycling:danmark-rundt and cycling:tour-de-france).",
	"fixed": [
		"Moved the cycling:renewi-tour entry into its id-sorted position in scripts/config/authority.json (content unchanged; pure relocation, 28 ins / 28 del). authority-schema.test.js 13/13 green; full npm test 1376/1376 green; JSON re-parses. PR #440."
	],
	"autoMergeEligible": true,
	"notes": [
		"Not a protected path (scripts/config/authority.json is AI-maintained/human-audited, not one of .github/workflows/**, scripts/hooks/**, or scripts/config/interests.json), so the fix is auto-merge eligible once the workflow re-gates tests.",
		"RECURRENCE WATCH: this id-sort contract broke on 2026-08-19 (resolved by a later config rewrite) and again now — root pattern is agents rewriting authority.json/sources.json and appending new competitions at the tail instead of sorted. A durable fix belongs to the research agent (sort-on-write) or a canonicalizer, not the mechanic patching each instance; flag for the improve agent to consider a write-time sort so this stops recurring.",
		"STILL BROKEN (unchanged from prior runs, protected-path, not fixable by this agent): deep-tier research escalation fails on bot-initiated runs (anthropics/claude-code-action@v1 'Workflow initiated by non-human actor' — .github/workflows/research-agent.yml needs allowed_bots: \"github-actions[bot]\" on all three action steps). The protect-automation hook blocks this unattended agent from editing that workflow. Ready-to-apply fix documented in the 2026-08-21 log; needs a human. This is the 3rd consecutive run to surface it via this log with no human action — a louder channel (GitHub issue) may be warranted.",
		"Non-bugs ruled out this run: node scripts/validate-events.js → 0 errors (warnings only: timezone-bleed, landing-page channel URLs WP-246, weak time/channel basis WP-242 — data-quality signals owned by research/verify, not mechanic breakage). Visual QA failures (32463657434 and prior) remain a separate max-turns=40 issue on an optional agent, not addressed here. Research agent failures line up with the deep-tier bot-actor break above, not new breakage."
	]
}

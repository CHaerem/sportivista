{
	"runAt": "2026-08-30T12:10:00Z",
	"action": "abandoned",
	"pr": null,
	"diagnosed": "TWO real, recurring breakages found, BOTH of whose only correct root-cause fix lives under .github/workflows/** — a protected automation path that scripts/hooks/protect-automation.js HARD-BLOCKS every CI agent (incl. self-repair) from editing. The Edit was blocked by the hook this run, so branch+PR is impossible; a human must author both. (1) RESEARCH ESCALATION LANE DEAD (~10 days): every research run DISPATCHED by scout/coverage-critic (`gh workflow run research-agent.yml`, via scripts/escalate-research.js) fails — the claude-code-action@v1 step aborts with `Workflow initiated by non-human actor: github-actions (type: Bot). Add bot to allowed_bots list or use '*' to allow all bots.` A GITHUB_TOKEN-issued dispatch runs as github-actions[bot], which the action rejects unless allow-listed (`allowed_bots: \"\"`). Confirmed run 33304960129 (08-30 09:48, event=workflow_dispatch — both deep Fable 5 + Opus 4.8 fallback rejected), 33248764003 (08-29), 33187753529 (08-28). SCHEDULED research (human actor) is unaffected and still runs, which is why the board keeps updating and no other signal flags it. (2) VISUAL-QA FAILS ON TURN BUDGET: the 08:00 scheduled run repeatedly dies at `--max-turns 40` — either 'Reached maximum number of turns (40)' mid-task (runs 32827302858 08-25, 32707452504 08-24, 32628049149 08-23, 32562050726 08-22, 32463657434 08-21, 32349103634 08-20) or 'Claude reported a successful result after 42 turns, exceeding the configured maximum of 40' (run 33255248354 08-29). The turn-heavy task (Playwright screenshots at 375/393/900px + vision analysis of each + log write + git pull/rebase/push) sits right at the 40 boundary; 40 is the second-lowest budget of all agents. Other health signals GREEN: full `npm test` 1525 passed / 87 files, `node scripts/validate-events.js` 0 errors (only known WP-246/WP-242/timezone-bleed warnings owned by research/verify), publish-freshness.json `fresh` (0h lag), usage-state.json green (session 0% / week 63%, unifiedStatus allowed). No open self-repair/ PR exists.",
	"fixed": [],
	"autoMergeEligible": false,
	"notes": [
		"THIRD CONSECUTIVE DETECTION of the research escalation breakage — the 08-28 and 08-29 self-repair runs both diagnosed this identical issue and abandoned (blocked by the same hook). It is STILL broken and STILL unauthored. This is an owner action, not another agent pass. FIX (human-authored, protected path): in .github/workflows/research-agent.yml add `allowed_bots: \"github-actions\"` under `with:` on all THREE `uses: anthropics/claude-code-action@v1` steps (standard-tier + deep Fable 5 + deep Opus 4.8 fallback). `\"*\"` also works but the tighter allow-list is preferred; only github-actions dispatches these and workflow_dispatch already requires actions:write. That is the exact remedy the error message names.",
		"CORRECTION to the prior (08-29) log's note that punted visual-qa to 'improve/visual-qa territory': the correct root-cause fix is a turn-budget bump (a .github/workflows/** change), which is a machine breakage — 'a workflow that keeps erroring' — squarely in this mechanic's remit, not a prompt-quality tweak. The recent 'successful result after 42 turns' case exceeded by only 2; the older cases were cut off mid-task. Trimming the prompt would degrade coverage (fewer widths checked) and is a hack, not the root fix. FIX (human-authored, protected path): in .github/workflows/visual-qa-agent.yml raise `--max-turns 40` to `--max-turns 60` (matching editorial + coverage-critic, whose tasks are comparable), giving clean headroom without excess.",
		"WHY NO PR for either: scripts/hooks/protect-automation.js is CI-only and blocks Write/Edit to .github/workflows/** (+ .github/actions/**, scripts/hooks/**, .claude/settings.json, scripts/merge-gate.js) for any unattended agent — 'A human must author changes to these files.' The hook is intentionally STRICTER than the self-repair prompt's 'make it, it'll wait for review'; surfacing to a human is the correct terminal outcome here, not a workaround.",
		"Non-bugs ruled out this run: CI web-tests failures (32852395226/32829942923, 08-25) — `npm test` is fully green now, already resolved/transient. publish-freshness fresh, validate-events 0 errors, usage green. No pipeline/fetcher/test-gate/data breakage to fix within non-protected paths."
	]
}

{
  "runAt": "2026-09-03T11:34:00.000Z",
  "action": "opened-pr",
  "pr": "https://github.com/CHaerem/sportivista/pull/458",
  "diagnosed": "The CI web-tests gate has been red since 2026-09-02 (failing runs 33716066944 / 33692045209 / 33673780090). Reproduced locally: tests/tracked-schema.test.js > 'has the expected top-level shape' fails with 'notes: expected false to be true' — tracked.json's `notes` field is a STRING, but the schema test requires it to be an Array. Root cause: the research agent regressed scripts/config/tracked.json at commit 748e26cfc (2026-09-02), rewriting `notes` from an array of per-run note strings into a single concatenated string that embeds prior runs via '--- TIDLIGERE:' separators. Every prior good commit (e.g. 68f6ec6b1) had a clean array of ~15 run-notes, none embedding the separator. Not quota/transient: usage-state.json is green (session 0%, week 11%); publish-freshness.json is 'fresh' (lag 0h).",
  "fixed": [
    "scripts/config/tracked.json: pure type correction — split the concatenated `notes` string back on its '--- TIDLIGERE:' separators into the 3 constituent run-notes, restoring the array-of-strings shape (most-recent-first). No content invented or dropped.",
    "scripts/agents/research.md: hardened the top-level-shape line to state explicitly that leagues/athletes/tournaments/notes are arrays and that `notes` is one run-note per element (never a concatenated string), so the agent does not re-regress it."
  ],
  "autoMergeEligible": true,
  "notes": [
    "Proof: tests/tracked-schema.test.js now 4/4 pass; full npm test 1525/1525 (87 files) green; node scripts/validate-events.js 0 errors (only pre-existing WP-242/WP-246 warnings). PR #458 touches no protected path (scripts/config/tracked.json + scripts/agents/research.md), so it is auto-merge eligible once the gate passes.",
    "STILL OUTSTANDING (protected path — human must author, unchanged from 2026-09-01/09-02 logs): escalation-triggered (workflow_dispatch by claude[bot]/github-actions[bot]) Research agent runs still fail because .github/workflows/research-agent.yml's claude-code-action steps have empty ALLOWED_BOTS. Re-confirmed against today's run 33734273496 (2026-09-03T08:36Z, event=workflow_dispatch, ALLOWED_BOTS empty). Fix = add `allowed_bots: \"*\"` (or `github-actions[bot]`) under claude_code_oauth_token: in all three action steps. Blocked by the protect-automation hook (.github/workflows/** is human-only); not authored here.",
    "Also noted for owner/improve (protected workflow-file fixes, not authored here): Visual QA runs keep failing with the max-turns limit ('exceeding the configured maximum of 40') — fix is raising --max-turns in visual-qa-agent.yml."
  ]
}

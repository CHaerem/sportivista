{
	"runAt": "2026-08-26T07:25:00Z",
	"action": "skipped-existing-pr",
	"pr": "https://github.com/CHaerem/sportivista/pull/442",
	"diagnosed": "SAME breakage as the 08-25 run, still on main: `npm test` is RED — tests/starter-packs.test.js (WP-162 guard) 'every starter-pack id is a LIVE entity in docs/data/entities.json' fails on 'esports-world-cup-cs2' (the CS2 starter pack in ios/Sportivista/Onboarding/StarterPacks.swift grounds on an EDITION-DERIVED entity id that vanishes off-season; the durable catalog id is `esports-world-cup`). The two recent 'CI' web-tests failures on main (runs 32852395226 @08-25 13:16, 32829942923 @09:04) are this same test. An open self-repair/ PR already fixes it exactly, so no new PR was opened per the contract.",
	"fixed": [],
	"autoMergeEligible": true,
	"notes": [
		"WHY IT'S STILL BROKEN ON MAIN (root cause of the stranded fix): PR #442 (branch self-repair/20260825-0730) is CLEAN + MERGEABLE with ALL checks green (web-tests, ios-tests, detect all pass) and carries no needs-review label — it is fully auto-merge-eligible. But its opening workflow run (32821331992) was CANCELLED: the 'Merge gate (re-test + protected paths + auto-merge)' step (#8) started 07:36:55 and was cancelled at 07:43:27 while mid-flight — ~30s BEFORE the macOS ios-tests check finally went green at 07:43:57. So merge-gate's `gh pr checks 442 --watch` never saw the check pass, the auto-merge never fired, and the correct fix has sat stranded for ~24h while the WP-162 test stays red on main.",
		"CONTRACT COMPLIANCE: an open self-repair/ PR fixes this exact breakage, so action=skipped-existing-pr and no duplicate PR was opened. The mechanic must NEVER merge or push to main itself, so #442 was NOT merged from here — it needs the merge-gate to re-fire (re-run the cancelled self-repair workflow, or re-trigger CI on the branch) or a human merge. The fix itself is verified: checked out #442's three ios/ changes are the same ones the 08-25 log describes, and all its checks are green.",
		"ACTION NEEDED (outside a mechanic's safe, non-merge scope): re-run/merge PR #442 so the fix lands. The underlying fragility — a self-repair PR that relies on a slow macOS ios-tests check can lose its auto-merge to a concurrency-cancel of the opening run when the next scheduled self-repair fires — is a workflow/merge-gate timing issue under .github/workflows/**, a protected path this agent cannot write. Flagged for human/improve follow-up.",
		"KNOWN RESIDUAL (unchanged from 08-25): if research re-books a discipline-suffixed CS2 edition next season, the primary id flips back to esports-world-cup-cs2 and this guard re-fails in-season; the durable end-state (unify discipline editions as altIds; WP-162 guard checks id ∪ altIds) is a build-entities/coverage-model change that ripples the golden feed-vectors — research/improve territory.",
		"PRE-EXISTING PROTECTED-PATH BREAKAGES (unchanged): (1) research-agent.yml deep-tier escalation dies on allowed_bots; (2) visual-qa-agent.yml hits error_max_turns at --max-turns 40. Both under .github/workflows/**, blocked from this agent by the protect-automation hook. These account for the recurring 'Research agent' / 'Visual QA agent' failures in `gh run list`.",
		"Non-bugs ruled out: usage-state.json green (session 5% / week 28%, unifiedStatus allowed — no quota attribution). validate-events.js → 0 errors (only known WP-246/WP-242/timezone warnings owned by research/verify). build-alert.json ok:true, 90 events."
	]
}

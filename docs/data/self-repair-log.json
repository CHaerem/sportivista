{
	"runAt": "2026-08-08T07:36:46Z",
	"action": "abandoned",
	"pr": null,
	"diagnosed": "REAL, SEVERE breakage (P0): GitHub Pages has not deployed since 2026-07-29T19:58 — the public site (sportivista.com) has served frozen content for ~10 days despite the static pipeline committing fresh data hourly. Root cause: preview-deploy run 30486657144 (push, sha 62b214a, 2026-07-29T19:58:24Z) is stuck in workflow status `waiting` — its `deploy` job never left the github-pages environment gate, and its deployment record 5664166505 is stuck in state `waiting`. Because preview-deploy uses `concurrency: pages-deploy` with `cancel-in-progress: false` (intentional, to avoid corrupting the Pages env mid-flight), a run wedged in `waiting` HOLDS the concurrency group indefinitely. Every subsequent deploy — the hourly static-pipeline workflow_call deploy AND every push-triggered preview-deploy — queues behind it and is then superseded/cancelled by the next arrival before it can start. Confirmed: last 60 preview-deploy runs all `cancelled` with zero jobs started; last successful github-pages deployment = 2026-07-29 (Pages deployments API). The `waiting` is orphaned, not a legitimate gate: pending_deployments shows wait_timer=0, reviewers=[] — a GitHub-side glitch on 07-29 left the run parked on the branch_policy evaluation and it never resolved.",
	"fixed": [],
	"autoMergeEligible": false,
	"notes": [
		"NOT self-repairable by a code PR: the breakage is an orphaned Actions run holding a concurrency lock, not a code/config defect. No smallest-change edit to preview-deploy.yml/static-pipeline.yml clears an already-wedged run, and `cancel-in-progress: false` is deliberate (flipping it to true reintroduces the mid-flight Pages-env corruption commit 3b42be176 fixed). timeout-minutes would not help — it bounds execution time, not time parked on an environment gate.",
		"REQUIRES A HUMAN ADMIN (owner) — this agent's token is a restricted integration token and got HTTP 403 on: `gh run cancel 30486657144`, force-cancel, review-pending-deployments (reject), and POST deployment status inactive. current_user_can_approve=false.",
		"REMEDY (one of): (a) `gh run cancel 30486657144` (or Force cancel in the Actions UI) to release the `pages-deploy` group; then re-run the static pipeline or `gh workflow run preview-deploy.yml` to publish. (b) If the cancel leaves deployment 5664166505 parked, mark it inactive: `gh api -X POST repos/CHaerem/sportivista/deployments/5664166505/statuses -f state=inactive`. Verify recovery via the github-pages deployments API showing a fresh `success` newer than 2026-07-29.",
		"PREVENTION (owner judgement, out of scope for an unattended code fix): the github-pages environment carries a custom branch_policy protection rule (allows only `main`) that parks deploys in `waiting`; since deploys only ever come from main and the main ruleset already governs it, removing that env protection rule would stop runs entering the gate that glitched here. This is a repo-settings change, not a workflow-file change, so it is left to the owner.",
		"Everything else is healthy: `npm test` green (1368/1368, 81 files), `node scripts/validate-events.js` 0 errors, build-alert.json ok (77 events, 06:45Z). The recent 2026-08-06 workflow failures (iOS Release, Scout, Usage monitor, Research, Editorial, Visual QA) were all transient GitHub infra ('The job was not acquired by Runner of type hosted') — not code bugs; usage-state.json is green (session 3%, week 21%), so these are not quota skips either. The old CI starter-packs failure (11 days ago) already passes now."
	]
}

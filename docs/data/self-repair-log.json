{
  "runAt": "2026-08-31T14:15:00.000Z",
  "action": "opened-pr",
  "pr": "https://github.com/CHaerem/sportivista/pull/456",
  "diagnosed": "Two WP-250 golf tee-hint tests in tests/dashboard-cards.test.js failed in CI (run 33340392821) and locally on 2026-08-31: 'renders the hint into the agenda row, before the channel' and 'lets the tournament yield to the tee time so the row keeps its height'. Root cause is a time-bomb fixture: tourChampionship() hardcoded a tournament window (time 2026-08-27 … endTime 2026-08-30). The two failing tests render via dash.eventRow, which uses the real clock; once the calendar passed 2026-08-30, finishedInfo marked the fixture done and dashboard.js:763 suppressed the tee hint for done events, so the row no longer contained ev-tee / 'Hovland ut 17:24'. The golfTeeHint(e, NOW) unit tests passed because golfTeeHint ignores time/endTime.",
  "fixed": ["tests/dashboard-cards.test.js: made the tourChampionship fixture window straddle now (now-1d … now+1d) so the tournament is in progress whatever day the suite runs; smallest root-cause fix (fixture, not production code)."],
  "autoMergeEligible": true,
  "notes": [
    "Change touches only tests/ — no protected path, so it is auto-merge eligible once tests re-pass.",
    "Proof: npx vitest run tests/dashboard-cards.test.js -> 116/116; full npm test -> 1525/1525 green.",
    "node scripts/validate-events.js passes (0 errors; warnings only).",
    "publish-freshness.json status was 'fresh' (lag 0h) — the published site is current, no ops fix needed.",
    "Ignored non-bugs: prior Research-agent failures line up with the quota governor, not code; Visual QA failures are the vision agent's domain, not mechanic breakage."
  ]
}

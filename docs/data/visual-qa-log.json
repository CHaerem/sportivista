{
  "checkedAt": "2026-07-30T10:23:02Z",
  "widths": [375, 393, 900],
  "verdict": "issues",
  "findings": [
    {
      "width": 375,
      "area": "assistant-fab overlapping agenda row (I DAG · Hradec Králové – Tromsø)",
      "issue": "The floating 'Assistent' button (fixed bottom-right, translucent + blurred pill) sits directly on top of the 19:00 'Hradec Králové – Tromsø' row. It visually occludes the middle of the team name itself — the row reads as 'H[icon] ★ Assistent Kralove – Tromsø' — hiding the fact that this is Norway's Europa League qualifier. This is the exact class of bug the checklist calls out: a fixture readable to code but not to an eye.",
      "severity": "high"
    },
    {
      "width": 393,
      "area": "assistant-fab overlapping agenda row (I DAG · Hradec Králové – Tromsø)",
      "issue": "Same fixed-position 'Assistent' pill lands on the same row one line lower here, covering the competition/channel subtext ('UEFA Europa League 2026/27 (kvalifisering) · VG TV') so the 'where to watch' channel is unreadable behind the button.",
      "severity": "high"
    },
    {
      "width": 900,
      "area": "assistant-fab",
      "issue": "At this width the button lands in the empty right-hand margin beside the agenda column (which is capped at 640px) and does not overlap any text — no issue here.",
      "severity": "low"
    }
  ],
  "notes": [
    "The Apple sign-in wall that blocked every check on the last two runs (2026-07-28/29) is gone this run: the dashboard content itself now renders and could be fully evaluated. (The screenshot harness serves a tokenless icloud-config.js per a comment in scripts/screenshot.js — noting this so a human knows why this run can suddenly see real content again.)",
    "The assistant-fab overlap is not a full-page-screenshot stitching quirk: the button is `position: fixed` with no transformed/filtered ancestor breaking its containing block (checked docs/css/cards.css + docs/index.html), and the capture viewport is a real 900px-tall browser viewport (scripts/screenshot.js), so this is how the pill sits relative to on-screen content at rest, not an artifact of tiling a tall page.",
    "Everything else checked out well at all three widths: no horizontal scroll/clipping, single quiet column, generous spacing, header/day-headings/footer intact and aligned, only Norwegian channels shown (TV 2, TV 2 Play, Viaplay, VG TV, HBO Max — no FOX/ESPN/Sky), times sensibly formatted, must-see accent dots used sparingly (4 of ~18 rows), national flags and club badges render as real icons (no broken-image boxes).",
    "Could not fully assess whether the small circular club badges are accurate crests vs. generic placeholders at this screenshot resolution — worth a closer look with a targeted crop if that matters."
  ]
}

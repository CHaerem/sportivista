{
	"runAt": "2026-07-30T11:16:00Z",
	"action": "opened-pr",
	"pr": "https://github.com/CHaerem/sportivista/pull/431",
	"fixed": [
		"375px HIGH: assistant FAB pill overlapped/fragmented the '19:00 Hradec Králové – Tromsø' team name",
		"393px HIGH: assistant FAB pill covered the same row's '· VG TV' channel subtext"
	],
	"notes": [
		"Root cause: on phones the agenda column is edge-to-edge, so the labelled fixed FAB has nowhere to sit but over a fixture. It already collapses to a glyph on scroll (WP-146) but is expanded at rest — exactly when visual-qa shot it.",
		"Fix: a @media (max-width: 460px) rule keeps the FAB in its existing collapsed glyph-only form regardless of scroll (reuses the .collapsed appearance, no new idiom). Wider screens keep the labelled pill in the empty right margin.",
		"Only docs/css/cards.css changed. Verified by re-screenshotting 375/393/900 + a clipped crop of the FAB region: team name + '· VG TV' fully readable at 375/393; 900 unchanged (pill in the margin, no overlap). npm test green (1368).",
		"Not merged — the workflow re-gates tests and auto-merges + deploys."
	]
}

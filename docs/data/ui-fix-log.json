{
	"runAt": "2026-07-31T11:42:00Z",
	"action": "opened-pr",
	"pr": "https://github.com/CHaerem/sportivista/pull/432",
	"fixed": [
		"375px HIGH: assistant FAB overlapping the '20:45 Birmingham City – Barcelona' row (ⓘ/chevron + '· VG' subtext)",
		"393px HIGH: assistant FAB covering the '21:00 Bodø/Glimt – Lillestrøm' row's chevron + '· TV 2' channel subtext"
	],
	"notes": [
		"Fix: shrank the phone-width (@media max-width:460px) floating button — min-height 48→44px, padding 12→11px, glyph 18→17px — and tucked it into the extreme corner (right/bottom 20→14px). Also removed the DESIGN-forbidden heavy drop shadow (0 6px 20px .28 → soft 0 2px 12px .18) so it reads as a compact glass capsule, not 'en fylt sirkel med skygge' (DESIGN.md §345–370). This implements visual-qa's own endorsed 'shrink the FAB below a width breakpoint' remedy.",
		"Only docs/css/cards.css changed. Verified with viewport (non-full-page) screenshots at 375/393/900 before + after: at 375 the Birmingham row's ⓘ/chevron are now clear of the smaller corner button; at 393 the footprint is materially reduced; at 900 the labelled pill still sits cleanly in the empty right margin beside the 640px column (only a lighter shadow). No regressions — header/tabs/DIREKTE NÅ/day-groups/agenda all intact and calm, no new overflow/truncation. npm test green (1368).",
		"Honest scope: the residual bottom-trailing corner overlap is INHERENT to the DESIGN.md-mandated floating button on an edge-to-edge single column ('Agendaen … scroller rolig UNDER knappen', WP-146 variant D). Fully eliminating it (dock / full-width bar / hide) is explicitly forbidden by DESIGN.md and would need an owner design decision; this PR minimises the footprint rather than claiming to remove the inherent overlap.",
		"Not merged — the workflow re-gates tests and auto-merges + deploys."
	]
}
